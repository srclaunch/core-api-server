// import {
//   Budget,
//   DataPoint,
//   IsoCurrencyCode,
//   UnofficialCurrencyCode,
//   User,
// } from '@budgetbloom/budgetbloom-types';

// import db from '../../../../models/core';

export async function createDataPoint({}: // budget_id,
// date,
// iso_currency_code,
// metric,
// value,
// user_id,
// unofficial_currency_code,
{
  // budget_id: Budget['id'];
  // date: Date;
  // iso_currency_code: IsoCurrencyCode;
  // metric: DataPoint['metric'];
  // value: number;
  // user_id: User['id'];
  // unofficial_currency_code: UnofficialCurrencyCode;
}): Promise<void> {
  // const { DataPoint: DataPointDb } = db;
  // await DataPointDb.create({
  //   BudgetId: budget_id,
  //   UserId: user_id,
  //   date,
  //   iso_currency_code,
  //   metric,
  //   unofficial_currency_code,
  //   value,
  // });
}

// export async function deleteAllUserSpendingCategories(
//   id: User['id'],
// ): Promise<void> {
//   const { DataPoint: DataPointDb } = db;

//   await DataPointDb.destroy({
//     where: {
//       UserId: id,
//     },
//   });
// }
