// import { PlaidWebhookEndpoints } from './plaid';
import { StripeWebhookEndpoints } from './stripe';

export const WebhookEndpoints = {
  // plaid: PlaidWebhookEndpoints.webhook,
  stripe: StripeWebhookEndpoints.webhook,
};
